# - Config file for the vision lib
# It defines the following variables
#
#  VISION_INCLUDE_DIRS - include directories
#  VISION_LIBRARY_DIR  - library directory (normally not used!)
#  VISION_LIBRARIES    - libraries to link against
 
# Tell the user project where to find our headers and libraries

set(VISION_INCLUDE_DIRS "@INSTALL_HEADERS_PATH@"
			"@EIGEN3_INCLUDE_DIR@" 
			"@ImageMagick_INCLUDE_DIRS@" )

set(VISION_LIBRARY_DIR  "@INSTALL_LIB_PATH@")

set(VISION_LIBRARIES    vision 
                        "@ImageMagick_LIBRARIES@" )
